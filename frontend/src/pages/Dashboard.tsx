import {useEffect,useState} from "react"; import {useNavigate} from "react-router-dom"; import AppLayout from "../layouts/AppLayout"; import Greeting from "../components/dashboard/Greeting"; import RecoveryStreak from "../components/dashboard/RecoveryStreak"; import RecoveryScore from "../components/dashboard/RecoveryScore"; import DashboardOverview from "../components/dashboard/DashboardOverview"; import QuickActions from "../components/dashboard/QuickActions"; import RecoveryStatistics from "../components/dashboard/RecoveryStatistics"; import {getDashboardSummary} from "../services/dashboardService"; import type {DashboardSummary} from "../types/dashboard";
export default function Dashboard(){const[d,setD]=useState<DashboardSummary|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const navigate=useNavigate();useEffect(()=>{getDashboardSummary().then(setD).catch(e=>setError(e instanceof Error?e.message:"Unable to load dashboard")).finally(()=>setLoading(false));},[]);if(loading)return <AppLayout><div className="flex min-h-[400px] items-center justify-center text-slate-500">Loading your recovery dashboard...</div></AppLayout>;if(!d)return <AppLayout><div className="rounded-3xl bg-white p-10 text-center shadow"><h2 className="text-2xl font-bold">Unable to load dashboard</h2><p className="mt-3 text-slate-500">{error||"Please try again."}</p><button onClick={()=>navigate("/login")} className="mt-6 rounded-xl bg-blue-900 px-5 py-3 font-semibold text-white">Return to login</button></div></AppLayout>;return <AppLayout><Greeting dashboard={d}/><div className="grid gap-6 lg:grid-cols-2"><RecoveryStreak dashboard={d}/><RecoveryScore dashboard={d}/></div><DashboardOverview dashboard={d}/><QuickActions/><RecoveryStatistics dashboard={d}/></AppLayout>}
