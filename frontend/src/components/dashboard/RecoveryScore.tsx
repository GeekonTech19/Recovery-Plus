import DashboardCard from "../DashboardCard"; import type {DashboardSummary} from "../../types/dashboard";
export default function RecoveryScore({dashboard}:{dashboard:DashboardSummary}){const score=dashboard.recovery.recoveryScore;const status=score>=80?"Excellent Progress":score>=60?"Good Progress":score>=40?"Keep Going":"Let's Build Your Recovery";const radius=65,circumference=2*Math.PI*radius,offset=circumference-score/100*circumference;return <DashboardCard title="🏆 Recovery Score"><div className="flex flex-col items-center"><div className="relative"><svg width="170" height="170" className="-rotate-90"><circle cx="85" cy="85" r={radius} stroke="#e5e7eb" strokeWidth="12" fill="none"/><circle cx="85" cy="85" r={radius} stroke="#059669" strokeWidth="12" fill="none" strokeLinecap="round" strokeDasharray={circumference} strokeDashoffset={offset} style={{transition:"stroke-dashoffset .8s ease"}}/></svg><div className="absolute inset-0 flex flex-col items-center justify-center"><span className="text-4xl font-bold text-blue-900">{score}%</span><span className="text-sm text-slate-500">Score</span></div></div><h2 className="mt-6 text-xl font-bold text-slate-800">{status}</h2><p className="mt-2 text-center text-slate-500">Average stress: <span className="font-semibold">{dashboard.recovery.averageStress}/10</span></p><p className="mt-2 text-center text-slate-500">Calculated from your recent recovery check-ins.</p></div></DashboardCard>}
