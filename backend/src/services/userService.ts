import prisma from "../config/prisma"; export async function updateUserPreferences(data:{userId:string;timezone?:string;locale?:string;language?:string}){return prisma.user.update({where:{id:data.userId},data:{timezone:data.timezone,locale:data.locale,language:data.language},select:{id:true,firstName:true,lastName:true,email:true,timezone:true,locale:true,language:true,createdAt:true}});}
